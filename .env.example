# Copy to .env and fill in locally. .env is gitignored; never commit real values.
# Placeholders only in this file.

# SQLite file lives under ./data/ , which is gitignored and created at runtime.
DATABASE_URL=sqlite:///./data/urls.db
