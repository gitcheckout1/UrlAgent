# Agent

Placeholder. Notes on the orchestrator's agent steps: what each node in the homemade DAG is
responsible for, what it reads and writes, and where the human approval gate sits. Approval and
release are human decisions and are never set automatically in code. To be filled in later.
